# requirements.txt — install all at once:
# pip install -r requirements.txt
# Python 3.13 compatible versions

# Core
numpy>=2.0.0
scipy>=1.13.0
opencv-python>=4.10.0.84
opencv-contrib-python>=4.10.0.84

# Image processing
scikit-image>=0.24.0
PyWavelets>=1.6.0
tifffile>=2024.5.22
Pillow>=10.3.0

# ML / DL
# YOLOv8 — auto-downloads yolov8n.pt on first use
ultralytics>=8.2.18

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# MLOps
dvc>=3.50.0
mlflow>=2.13.0
evidently>=0.4.30

# UI
streamlit>=1.35.0

# Config
pyyaml>=6.0.1

# Optional — for tifffile TIFF support (requires MSVC)
# imagecodecs==2024.1.1